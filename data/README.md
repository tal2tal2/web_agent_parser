## `data/`

This folder holds runtime artifacts and experiment inputs/outputs.

- `raw/`: raw HTML snapshots (downloads or captures)
- `processed/`: structured parses (JSON/JSONL/SQLite)

Most contents are git-ignored by default; only this README and `.gitkeep` files are committed.


